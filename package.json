{
  "devDependencies": {
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "concurrently": "^6.2.0",
    "json-server": "^0.16.3"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html",
    "start": "concurrently npm:start:*"
  }
}
